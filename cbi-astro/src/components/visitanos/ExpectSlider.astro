---
import { BookOpen, Music, Users } from "@lucide/astro"
import SliderArrow from "../common/SliderArrow.astro"
import "swiper/css"
import "swiper/css/navigation"
---

<section class="py-18 px-6 lg:px-16">
  <div class="max-w-6xl mx-auto">
    <!-- Header with title and navigation arrows -->
    <div class="flex items-center justify-between gap-4 mb-12">
      <h2
        class="font-bebas text-4xl sm:text-5xl lg:text-6xl text-primary text-center">
        Qué Esperar de nuestros servicios
      </h2>
      <div class="flex gap-3">
        <SliderArrow direction="prev" class="expect-swiper-prev" />
        <SliderArrow direction="next" class="expect-swiper-next" />
      </div>
    </div>

    <!-- Slider Container -->
    <div class="expect-swiper-container relative">
      <div class="swiper expect-swiper">
        <div class="swiper-wrapper">
          <!-- Card 1 -->
          <div class="swiper-slide">
            <div
              class="group text-center p-8 rounded-2xl bg-accent hover:bg-accent/80 transition-all duration-300 h-full">
              <div
                class="w-16 h-16 mx-auto mb-6 rounded-full bg-primary/10 flex items-center justify-center group-hover:bg-primary transition-colors duration-300">
                <Users
                  class="w-8 h-8 text-primary group-hover:text-white transition-colors duration-300"
                />
              </div>
              <h3 class="text-xl font-bold text-white mb-3">Comunidad</h3>
              <p class="text-white/80 leading-relaxed">
                Somos una comunidad cálida donde todos somos bienvenidos.
              </p>
            </div>
          </div>

          <!-- Card 2 -->
          <div class="swiper-slide">
            <div
              class="group text-center p-8 rounded-2xl bg-accent hover:bg-accent/80 transition-all duration-300 h-full">
              <div
                class="w-16 h-16 mx-auto mb-6 rounded-full bg-primary/10 flex items-center justify-center group-hover:bg-primary transition-colors duration-300">
                <Music
                  class="w-8 h-8 text-primary group-hover:text-white transition-colors duration-300"
                />
              </div>
              <h3 class="text-xl font-bold text-white mb-3">Adoración</h3>
              <p class="text-white/80 leading-relaxed">
                Una Adoración que alinea nuestro corazón con el Padre y nos
                prepara para escuchar Su Voz
              </p>
            </div>
          </div>

          <!-- Card 3 -->
          <div class="swiper-slide">
            <div
              class="group text-center p-8 rounded-2xl bg-accent hover:bg-accent/80 transition-all duration-300 h-full">
              <div
                class="w-16 h-16 mx-auto mb-6 rounded-full bg-primary/10 flex items-center justify-center group-hover:bg-primary transition-colors duration-300">
                <BookOpen
                  class="w-8 h-8 text-primary group-hover:text-white transition-colors duration-300"
                />
              </div>
              <h3 class="text-xl font-bold text-white mb-3">Palabra</h3>
              <p class="text-white/80 leading-relaxed">
                Mensajes bíblicos y prácticos que puedes aplicar en tu vida.
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  import Swiper from "swiper"
  import { Navigation } from "swiper/modules"

  document.addEventListener("DOMContentLoaded", () => {
    new Swiper(".expect-swiper", {
      modules: [Navigation],
      slidesPerView: 1,
      spaceBetween: 24,
      navigation: {
        prevEl: ".expect-swiper-prev",
        nextEl: ".expect-swiper-next",
      },
      breakpoints: {
        640: {
          slidesPerView: 2,
        },
        1024: {
          slidesPerView: 3,
        },
      },
    })
  })
</script>

<style>
  /* Expect Swiper Styles */
  .expect-swiper-container {
    position: relative;
  }

  .expect-swiper {
    overflow: hidden;
  }

  .expect-swiper .swiper-slide {
    height: auto;
  }
</style>
