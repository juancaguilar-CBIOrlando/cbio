---
import Layout from "../layouts/Layout.astro"
import { Image } from "astro:assets"
import {
  ArrowLeft,
  Calendar,
  MapPin,
  ArrowRight,
  ChevronRight,
  Users,
  Clock,
  Heart,
  Sparkles,
  RefreshCw,
  CheckCircle,
  AlertCircle,
} from "@lucide/astro"
import { events, getFeaturedEvents } from "../helpers/eventsData.js"
import { address } from "../constants.js"

// Get the Encuentros CBI event data for dynamic info (date, image, etc.)
const event = events.find((e) => e.title === "Encuentros CBI")!

// Get other featured events for recommendations
const otherEvents = getFeaturedEvents()
  .filter((e) => e.id !== event.id)
  .slice(0, 3)
---

<Layout title="Encuentros CBI - CBI Orlando" navLogo="white">
  <!-- Hero Section -->
  <section class="relative min-h-[70vh] flex items-end overflow-hidden">
    {
      event.image ? (
        <Image
          src={event.image}
          alt="Encuentros CBI"
          width={1920}
          height={1080}
          quality={85}
          class="absolute inset-0 w-full h-full object-cover"
          loading="eager"
        />
      ) : (
        <div class="absolute inset-0 w-full h-full bg-linear-to-br from-gray-800 to-gray-950" />
      )
    }

    <!-- Gradient overlay -->
    <div
      class="absolute inset-0 bg-linear-to-t from-black via-black/60 to-black/30">
    </div>

    <!-- Back button -->
    <a
      href="/eventos"
      class="absolute top-24 left-6 z-20 inline-flex items-center gap-2 text-white/80 hover:text-white transition-colors text-sm font-medium group">
      <ArrowLeft
        class="w-4 h-4 transition-transform group-hover:-translate-x-1"
      />
      Volver a eventos
    </a>

    <!-- Hero content -->
    <div class="relative z-10 w-full pb-16 pt-40">
      <div class="max-w-6xl mx-auto px-6">
        <!-- Category badge -->
        <span
          class="inline-block bg-primary text-white text-xs font-semibold px-4 py-1.5 rounded-full uppercase tracking-wider mb-4">
          {event.category}
        </span>

        <h1
          class="text-4xl md:text-6xl lg:text-7xl font-bold text-white mb-4 font-bebas">
          {event.title}
        </h1>

        <p class="text-primary text-lg md:text-xl font-medium mb-6">
          {event.tagline}
        </p>

        <!-- Event meta -->
        <div class="flex flex-wrap gap-6 text-white/80">
          {
            event.date && (
              <div class="flex items-center gap-2">
                <Calendar class="w-5 h-5 text-primary" />
                <span class="text-lg">{event.date}</span>
              </div>
            )
          }
          <div class="flex items-center gap-2">
            <MapPin class="w-5 h-5 text-primary" />
            <span class="text-lg">{event.location}</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Content Section -->
  <section class="py-16 bg-white">
    <div class="max-w-6xl mx-auto px-6">
      <div class="grid lg:grid-cols-3 gap-12">
        <!-- Main Content -->
        <div class="lg:col-span-2">
          <h2 class="text-3xl font-bold text-accent mb-6 font-bebas">
            Acerca de {event.title}
          </h2>

          <p class="text-gray-600 text-lg leading-relaxed mb-8">
            {event.description}
          </p>

          <!-- Introduction Card -->
          <div
            class="bg-linear-to-br from-primary/5 to-accent/5 border border-primary/10 rounded-2xl mb-8">
            <div class="p-6">
              <p class="text-gray-700 text-lg leading-relaxed">
                Los Encuentros CBI son experiencias diseñadas para llevarte a un
                encuentro personal y transformador con Dios. Durante este
                retiro, tendrás la oportunidad de experimentar sanidad interior,
                liberación y renovación espiritual.
              </p>
            </div>
          </div>

          <!-- What to Expect Section -->
          <div class="mb-10">
            <h3
              class="text-2xl sm:text-3xl font-bold text-accent mb-6 font-bebas flex items-center gap-3">
              ¿Qué puedes esperar?
            </h3>

            <div class="grid sm:grid-cols-2 gap-4">
              <!-- Card 1 -->
              <div
                class="bg-white rounded-2xl border border-gray-100 shadow-sm hover:shadow-md transition-shadow">
                <div class="p-5">
                  <div class="flex items-start gap-3">
                    <div
                      class="w-10 h-10 bg-primary/10 rounded-lg flex items-center justify-center shrink-0">
                      <Heart class="w-5 h-5 text-primary" />
                    </div>
                    <div>
                      <h4 class="font-bold text-accent mb-1">
                        Sanidad Interior
                      </h4>
                      <p class="text-gray-600 text-sm leading-relaxed">
                        Tiempo de ministración personal donde Dios trae sanidad
                        a áreas heridas de tu corazón.
                      </p>
                    </div>
                  </div>
                </div>
              </div>

              <!-- Card 2 -->
              <div
                class="bg-white rounded-2xl border border-gray-100 shadow-sm hover:shadow-md transition-shadow">
                <div class="p-5">
                  <div class="flex items-start gap-3">
                    <div
                      class="w-10 h-10 bg-primary/10 rounded-lg flex items-center justify-center shrink-0">
                      <Sparkles class="w-5 h-5 text-primary" />
                    </div>
                    <div>
                      <h4 class="font-bold text-accent mb-1">Liberación</h4>
                      <p class="text-gray-600 text-sm leading-relaxed">
                        Rompimiento de ataduras y cadenas que te han mantenido
                        cautivo.
                      </p>
                    </div>
                  </div>
                </div>
              </div>

              <!-- Card 3 -->
              <div
                class="bg-white rounded-2xl border border-gray-100 shadow-sm hover:shadow-md transition-shadow">
                <div class="p-5">
                  <div class="flex items-start gap-3">
                    <div
                      class="w-10 h-10 bg-primary/10 rounded-lg flex items-center justify-center shrink-0">
                      <RefreshCw class="w-5 h-5 text-primary" />
                    </div>
                    <div>
                      <h4 class="font-bold text-accent mb-1">Renovación</h4>
                      <p class="text-gray-600 text-sm leading-relaxed">
                        Un nuevo comienzo en tu relación con Dios.
                      </p>
                    </div>
                  </div>
                </div>
              </div>

              <!-- Card 4 -->
              <div
                class="bg-white rounded-2xl border border-gray-100 shadow-sm hover:shadow-md transition-shadow">
                <div class="p-5">
                  <div class="flex items-start gap-3">
                    <div
                      class="w-10 h-10 bg-primary/10 rounded-lg flex items-center justify-center shrink-0">
                      <Users class="w-5 h-5 text-primary" />
                    </div>
                    <div>
                      <h4 class="font-bold text-accent mb-1">Comunidad</h4>
                      <p class="text-gray-600 text-sm leading-relaxed">
                        Conexión profunda con otros creyentes en un ambiente de
                        amor y aceptación.
                      </p>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>

          <!-- Event Details Section -->
          <div class="mb-8">
            <h3
              class="text-2xl font-bold text-accent mb-6 font-bebas flex items-center gap-3">
              <span
                class="w-10 h-10 bg-accent/10 rounded-full flex items-center justify-center">
                <span class="text-accent text-lg">📋</span>
              </span>
              Detalles del Evento
            </h3>

            <div class="bg-slate-50 rounded-2xl p-6 border border-gray-100">
              <ul class="space-y-4">
                <li class="flex items-center gap-4">
                  <div class="badge badge-primary badge-lg gap-2 px-4 py-3">
                    <Clock class="w-4 h-4" />
                    Duración
                  </div>
                  <span class="text-gray-700 font-medium"
                    >Fin de semana completo (viernes a domingo)</span
                  >
                </li>
                <li class="flex items-center gap-4">
                  <div class="badge badge-primary badge-lg gap-2 px-4 py-3">
                    <CheckCircle class="w-4 h-4" />
                    Incluye
                  </div>
                  <span class="text-gray-700 font-medium"
                    >Hospedaje, alimentación y materiales</span
                  >
                </li>
                <li class="flex items-center gap-4">
                  <div class="badge badge-accent badge-lg gap-2 px-4 py-3">
                    <AlertCircle class="w-4 h-4" />
                    Importante
                  </div>
                  <span class="text-gray-700 font-medium"
                    >Cupo limitado - ¡Inscríbete pronto!</span
                  >
                </li>
              </ul>
            </div>
          </div>

          <!-- Testimonial/Quote -->
          <div class="bg-accent text-white rounded-2xl">
            <div class="p-6">
              <blockquote class="text-lg italic leading-relaxed">
                "Encontré libertad en áreas de mi vida que pensé nunca
                cambiarían. El Encuentro CBI fue el comienzo de mi
                transformación."
              </blockquote>
              <p class="text-primary font-semibold mt-3">
                — Testimonio de participante
              </p>
            </div>
          </div>
        </div>

        <!-- Sidebar -->
        <div class="lg:col-span-1">
          <!-- Registration Card -->
          <div
            class="sticky top-24 bg-slate-50 rounded-2xl p-6 border border-gray-100 shadow-sm">
            <h3 class="text-xl font-bold text-accent mb-4 font-bebas">
              Información del Evento
            </h3>

            <div class="space-y-4 mb-6">
              <div class="flex items-start gap-3">
                <div
                  class="w-10 h-10 bg-primary/10 rounded-full flex items-center justify-center shrink-0">
                  <Calendar class="w-5 h-5 text-primary" />
                </div>
                <div>
                  <p class="text-sm text-gray-500 font-medium">Fecha</p>
                  <p class="text-gray-800 font-semibold">
                    {event.date || "Por confirmar"}
                  </p>
                </div>
              </div>

              {
                event.duration && (
                  <div class="flex items-start gap-3">
                    <div class="w-10 h-10 bg-primary/10 rounded-full flex items-center justify-center shrink-0">
                      <Clock class="w-5 h-5 text-primary" />
                    </div>
                    <div>
                      <p class="text-sm text-gray-500 font-medium">Duración</p>
                      <p class="text-gray-800 font-semibold">
                        {event.duration}
                      </p>
                    </div>
                  </div>
                )
              }

              <div class="flex items-start gap-3">
                <div
                  class="w-10 h-10 bg-primary/10 rounded-full flex items-center justify-center shrink-0">
                  <MapPin class="w-5 h-5 text-primary" />
                </div>
                <div>
                  <p class="text-sm text-gray-500 font-medium">Ubicación</p>
                  <p class="text-gray-800 font-semibold">{event.location}</p>
                  <p class="text-gray-500 text-sm">{address}</p>
                </div>
              </div>

              <div class="flex items-start gap-3">
                <div
                  class="w-10 h-10 bg-primary/10 rounded-full flex items-center justify-center shrink-0">
                  <Users class="w-5 h-5 text-primary" />
                </div>
                <div>
                  <p class="text-sm text-gray-500 font-medium">Categoría</p>
                  <p class="text-gray-800 font-semibold">{event.category}</p>
                </div>
              </div>
            </div>

            <!-- CTA Button -->
            <a
              href="/conectate"
              class="w-full inline-flex items-center justify-center gap-2 bg-primary text-white font-semibold py-4 px-6 rounded-full hover:bg-primary/90 transition-all duration-300 hover:scale-[1.02] group">
              Quiero Participar
              <ArrowRight
                class="w-5 h-5 transition-transform group-hover:translate-x-1"
              />
            </a>

            <!-- Secondary action -->
            <a
              href="/conectate"
              class="w-full mt-3 inline-flex items-center justify-center gap-2 bg-transparent border-2 border-accent text-accent font-semibold py-3 px-6 rounded-full hover:bg-accent hover:text-white transition-all duration-300">
              ¿Tienes preguntas?
            </a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Other Events Section -->
  {
    otherEvents.length > 0 && (
      <section class="py-16 bg-slate-50">
        <div class="max-w-6xl mx-auto px-6">
          <div class="flex items-center justify-between mb-10">
            <div>
              <span class="inline-block bg-accent/10 text-accent px-4 py-1 rounded-full text-sm font-semibold mb-2">
                MÁS EVENTOS
              </span>
              <h2 class="text-3xl md:text-4xl font-bold text-accent font-bebas">
                También Te Puede Interesar
              </h2>
            </div>
            <a
              href="/eventos"
              class="hidden md:inline-flex items-center gap-2 text-primary font-semibold hover:gap-3 transition-all">
              Ver todos
              <ChevronRight class="w-5 h-5" />
            </a>
          </div>

          <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
            {otherEvents.map((otherEvent) => {
              const eventLink =
                otherEvent.hasDetailPage && otherEvent.pageUrl
                  ? otherEvent.pageUrl
                  : otherEvent.registrationLink || null
              const isExternal =
                !otherEvent.hasDetailPage && otherEvent.registrationLink

              return (
                <a
                  href={eventLink || "#"}
                  target={isExternal ? "_blank" : undefined}
                  rel={isExternal ? "noopener noreferrer" : undefined}
                  class="group bg-white rounded-2xl overflow-hidden shadow-sm hover:shadow-lg transition-all duration-300 hover:-translate-y-1">
                  <div class="relative h-48 overflow-hidden">
                    {otherEvent.image ? (
                      <Image
                        src={otherEvent.image}
                        alt={otherEvent.title}
                        width={400}
                        height={300}
                        class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-110"
                      />
                    ) : (
                      <div class="w-full h-full bg-linear-to-br from-gray-700 to-gray-900 flex items-center justify-center">
                        <span class="text-gray-500 text-sm">
                          Imagen próximamente
                        </span>
                      </div>
                    )}
                    <div class="absolute inset-0 bg-linear-to-t from-black/60 to-transparent" />
                    <span class="absolute top-3 left-3 bg-primary text-white text-xs font-semibold px-3 py-1 rounded-full">
                      {otherEvent.category}
                    </span>
                  </div>
                  <div class="p-5">
                    <h3 class="text-xl font-bold text-accent mb-2 group-hover:text-primary transition-colors font-bebas">
                      {otherEvent.title}
                    </h3>
                    {otherEvent.date && (
                      <p class="text-gray-500 text-sm flex items-center gap-2">
                        <Calendar class="w-4 h-4" />
                        {otherEvent.date}
                      </p>
                    )}
                  </div>
                </a>
              )
            })}
          </div>

          <div class="mt-8 text-center md:hidden">
            <a
              href="/eventos"
              class="inline-flex items-center gap-2 text-primary font-semibold">
              Ver todos los eventos
              <ChevronRight class="w-5 h-5" />
            </a>
          </div>
        </div>
      </section>
    )
  }

  <!-- CTA Section -->
  <section class="py-20 bg-accent relative overflow-hidden">
    <!-- Decorative elements -->
    <div
      class="absolute top-0 left-0 w-64 h-64 bg-primary/10 rounded-full -translate-x-1/2 -translate-y-1/2">
    </div>
    <div
      class="absolute bottom-0 right-0 w-96 h-96 bg-primary/5 rounded-full translate-x-1/3 translate-y-1/3">
    </div>

    <div class="relative z-10 max-w-3xl mx-auto px-6 text-center">
      <h2 class="text-4xl md:text-5xl font-bold text-white mb-4 font-bebas">
        ¿Listo para un <span class="text-primary">encuentro</span> con Dios?
      </h2>
      <p class="text-white/70 text-lg mb-8">
        Este fin de semana puede cambiar tu vida. Experimenta la libertad,
        sanidad y renovación que solo Dios puede darte.
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a
          href="/conectate"
          class="inline-flex items-center justify-center gap-2 px-8 py-4 bg-primary text-white font-semibold rounded-full hover:bg-primary/90 transition-all duration-300 hover:scale-105">
          Contáctanos
          <ArrowRight class="w-5 h-5" />
        </a>
        <a
          href="/visitanos"
          class="inline-flex items-center justify-center px-8 py-4 bg-transparent border-2 border-white text-white font-semibold rounded-full hover:bg-white hover:text-accent transition-all duration-300">
          Planea Tu Visita
        </a>
      </div>
    </div>
  </section>
</Layout>
