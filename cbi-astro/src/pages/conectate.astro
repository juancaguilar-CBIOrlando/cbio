---
import Layout from "../layouts/Layout.astro"
import { address, phone, email, serviceTimes } from "../constants"
---
<Layout title="Conectate - CBI Orlando">
  <div class="container mx-auto px-4 py-16 mt-16">
    <div class="max-w-3xl mx-auto">
      <h1 class="text-4xl md:text-5xl font-bold mb-6 text-center text-gray-900">
        Conectate con Nosotros
      </h1>
      <p class="text-xl text-gray-700 mb-12 text-center">
        Estamos aquí para ayudarte en tu relación con Dios. ¡Nos encantaría conocerte!
      </p>

      <div class="space-y-8">
        <!-- New Visitor Section -->
        <!-- FIXME: add a image and the text should be on top of the image -->
        <div class="bg-blue-200 backdrop-blur-md rounded-lg p-8 shadow-lg flex flex-col items-center justify-center">
          <h2 class="text-2xl font-semibold mb-2 text-black">Soy Nuevo</h2>
          <p class="text-gray-900 text-center mb-4">
            Si es tu primera vez visitándonos, queremos darte la bienvenida. Estamos aquí para ayudarte a crecer en tu relación con Dios.
          </p>
          <a 
            href="/visitanos" 
            class="inline-block bg-[#b81516] text-white px-6 py-3 rounded-md font-semibold hover:bg-[#9a1112] transition-colors"
          >
            Cómo Llegar
          </a>
        </div>

        <!-- Contact Information -->
        <div class="bg-gray-50 rounded-lg p-8">
          <h2 class="text-2xl font-semibold mb-4 text-gray-900">Información de Contacto</h2>
          <div class="space-y-4 text-gray-700">
            <div>
              <h3 class="font-semibold mb-2">Dirección</h3>
              <p>{address}</p>
            </div>
            <div>
              <h3 class="font-semibold mb-2">Teléfono</h3>
              <p>{phone}</p>
            </div>
            <div>
              <h3 class="font-semibold mb-2">Email</h3>
              <p>{email}</p>
            </div>
          </div>
        </div>

        <!-- Services Info -->
        <div class="flex flex-col items-center justify-center rounded-lg p-8">
          <h2 class="text-3xl font-semibold mb-6 text-gray-900 font-bebas">Únete a Nuestros Servicios</h2>
          <div class="w-full grid grid-cols-1 md:grid-cols-[repeat(auto-fit,minmax(250px,1fr))] lg:grid-cols-[repeat(auto-fit,minmax(300px,1fr))] gap-4">
            {serviceTimes.map((service) => (
              // FIXME: add the image for each service
              <div class="bg-white rounded-lg p-8 shadow-md shadow-blue-200 flex flex-col items-center justify-center">
                <img src={service.image} alt={service.name} class="w-full h-48 object-cover rounded-lg mb-4" />
                <h3 class="text-xl font-semibold mb-2 text-gray-900">{service.name}</h3>
                <p class="text-gray-700">
                  <span class="font-medium">{service.day}</span> {service.times.join(" & ")}
                </p>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  </div>
</Layout>

