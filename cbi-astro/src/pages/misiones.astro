---
import { Image } from "astro:assets"
import Layout from "../layouts/Layout.astro"
import MissionSection from "../components/missions/MissionSection.astro"
import ImpactSlider from "../components/missions/ImpactSlider.astro"
import {
  missionsSlides,
  impactSlides,
  misionesHeroBg,
} from "../helpers/missionsData.js"
---

<Layout navLogo="white" title="Misiones">
  <main class="bg-white">
    <header class="relative min-h-[70vh] flex items-center">
      <div class="absolute inset-0">
        <Image
          src={misionesHeroBg}
          alt="Misiones"
          width={1920}
          height={1080}
          quality={75}
          class="w-full h-full object-cover"
          loading="eager"
        />
        <div
          class="absolute inset-0 bg-linear-to-t from-black/80 via-black/55 to-black/10">
        </div>
      </div>

      <div class="relative z-10 w-full px-6 lg:px-16 pt-28 pb-16">
        <div class="max-w-6xl mx-auto sm:pt-24">
          <p
            class="text-white/80 font-semibold tracking-widest text-center uppercase text-xl">
            Misiones
          </p>
          <h1
            class="font-bebas text-5xl sm:text-7xl lg:text-8xl text-white text-center leading-[0.9] mt-3">
            Creemos, <span class="text-primary">Amamos</span> y Servimos.
            <br />
          </h1>
        </div>
      </div>
    </header>

    <MissionSection
      eyebrow="Misiones"
      title="Llevamos ayuda y esperanza a comunidades vulnerables."
      description="Tu aporte permite que llevemos el amor de Cristo a cada lugar. ¡Gracias por tu generosidad!"
      ctaText="Donar"
      ctaHref="/donation"
      sliderName="misiones"
      slides={missionsSlides}
    />

    <section class="py-18 px-6 lg:px-16 bg-gray-50">
      <div class="max-w-6xl mx-auto">
        <div class="mb-10">
          <h2
            class="font-bebas text-5xl sm:text-6xl lg:text-7xl leading-[0.95] text-black">
            Impacto de Misiones
          </h2>
          <p class="mt-4 text-black/70 text-lg leading-relaxed max-w-3xl">
            Mira lo que Dios está haciendo y únete apoyando con oración,
            donación o sirviendo.
          </p>
        </div>

        <ImpactSlider
          name="impacto-misiones"
          slides={impactSlides}
          ariaLabel="Impacto"
        />
      </div>
    </section>
  </main>
</Layout>
